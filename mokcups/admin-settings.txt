import React, { useState, useEffect } from 'react';
import { 
  Settings, 
  Code, 
  BarChart, 
  Smartphone, 
  Save, 
  ShieldCheck, 
  AlertTriangle,
  RefreshCw,
  Info
} from 'lucide-react';

const AdminSettings = () => {
  const [settings, setSettings] = useState({
    adsense_id: 'ca-pub-XXXXXXXXXXXXXXXX',
    analytics_id: 'G-XXXXXXXXXX',
    admob_id: 'ca-app-pub-XXXXXXXXXXXXXXXX',
    is_ads_enabled: true,
    maintenance_mode: false
  });

  const [isSaving, setIsSaving] = useState(false);
  const [status, setStatus] = useState(null); // 'success' | 'error'

  const handleSave = () => {
    setIsSaving(true);
    // Simulación de guardado en Supabase (tabla site_settings)
    setTimeout(() => {
      setIsSaving(false);
      setStatus('success');
      setTimeout(() => setStatus(null), 3000);
    }, 1000);
  };

  return (
    <div className="min-h-screen bg-slate-50 p-4 md:p-10 font-sans text-slate-900">
      <div className="max-w-4xl mx-auto">
        
        <header className="mb-10 flex flex-col md:flex-row md:items-center justify-between gap-4">
          <div>
            <h1 className="text-3xl font-black tracking-tight">Configuración del Sistema</h1>
            <p className="text-slate-500">Gestiona la monetización, analítica y parámetros globales.</p>
          </div>
          <button 
            onClick={handleSave}
            disabled={isSaving}
            className="bg-slate-900 text-white px-8 py-3 rounded-2xl font-bold flex items-center gap-2 hover:bg-slate-800 transition-all shadow-xl shadow-slate-200 disabled:opacity-50"
          >
            {isSaving ? <RefreshCw className="animate-spin" size={18} /> : <Save size={18} />}
            Guardar Cambios
          </button>
        </header>

        {status === 'success' && (
          <div className="mb-8 p-4 bg-emerald-500 text-white rounded-2xl flex items-center gap-3 animate-in fade-in slide-in-from-top-4">
            <ShieldCheck size={20} />
            <span className="font-bold">Configuración actualizada. Los cambios se reflejarán en la Web y App móvil.</span>
          </div>
        )}

        <div className="grid grid-cols-1 gap-8">
          
          {/* SECCIÓN: MONETIZACIÓN WEB */}
          <section className="bg-white p-8 rounded-[40px] border border-slate-200 shadow-sm">
            <div className="flex items-center gap-3 mb-6">
              <div className="p-3 bg-yellow-100 text-yellow-600 rounded-2xl"><Code size={24}/></div>
              <div>
                <h3 className="font-bold text-lg leading-none">Google AdSense (Web)</h3>
                <p className="text-xs text-slate-400 mt-1">Se desactivará automáticamente para usuarios con Plan Pro.</p>
              </div>
            </div>

            <div className="space-y-4">
              <div>
                <label className="text-xs font-black uppercase text-slate-400 mb-2 block">ID de Cliente (Publisher ID)</label>
                <input 
                  type="text" 
                  value={settings.adsense_id}
                  onChange={(e) => setSettings({...settings, adsense_id: e.target.value})}
                  className="w-full p-4 bg-slate-50 border border-slate-100 rounded-2xl outline-none focus:ring-2 focus:ring-yellow-400 font-mono text-sm"
                  placeholder="ca-pub-..."
                />
              </div>
              <div className="flex items-center gap-3 p-4 bg-slate-50 rounded-2xl border border-slate-100">
                <input 
                  type="checkbox" 
                  checked={settings.is_ads_enabled}
                  onChange={(e) => setSettings({...settings, is_ads_enabled: e.target.checked})}
                  className="w-5 h-5 accent-yellow-400"
                />
                <span className="text-sm font-bold text-slate-600">Activar anuncios en toda la plataforma (Nivel Free)</span>
              </div>
            </div>
          </section>

          {/* SECCIÓN: ANALYTICS */}
          <section className="bg-white p-8 rounded-[40px] border border-slate-200 shadow-sm">
            <div className="flex items-center gap-3 mb-6">
              <div className="p-3 bg-blue-100 text-blue-600 rounded-2xl"><BarChart size={24}/></div>
              <div>
                <h3 className="font-bold text-lg leading-none">Global Analytics</h3>
                <p className="text-xs text-slate-400 mt-1">Rastrea eventos de usuario en Web y Mobile App.</p>
              </div>
            </div>

            <div>
              <label className="text-xs font-black uppercase text-slate-400 mb-2 block">Google Analytics ID (G-XXXX)</label>
              <input 
                type="text" 
                value={settings.analytics_id}
                onChange={(e) => setSettings({...settings, analytics_id: e.target.value})}
                className="w-full p-4 bg-slate-50 border border-slate-100 rounded-2xl outline-none focus:ring-2 focus:ring-yellow-400 font-mono text-sm"
                placeholder="G-XXXXXXXXXX"
              />
            </div>
          </section>

          {/* SECCIÓN: MOBILE APP ADMOB */}
          <section className="bg-white p-8 rounded-[40px] border border-slate-200 shadow-sm">
            <div className="flex items-center gap-3 mb-6">
              <div className="p-3 bg-purple-100 text-purple-600 rounded-2xl"><Smartphone size={24}/></div>
              <div>
                <h3 className="font-bold text-lg leading-none">Google AdMob (Mobile App)</h3>
                <p className="text-xs text-slate-400 mt-1">Controla los anuncios en la App de Android/PlayStore.</p>
              </div>
            </div>

            <div className="space-y-4">
              <div>
                <label className="text-xs font-black uppercase text-slate-400 mb-2 block">App ID / Unit ID (Android)</label>
                <input 
                  type="text" 
                  value={settings.admob_id}
                  onChange={(e) => setSettings({...settings, admob_id: e.target.value})}
                  className="w-full p-4 bg-slate-50 border border-slate-100 rounded-2xl outline-none focus:ring-2 focus:ring-yellow-400 font-mono text-sm"
                  placeholder="ca-app-pub-..."
                />
              </div>
              <div className="flex items-start gap-3 p-4 bg-yellow-50 rounded-2xl border border-yellow-100">
                <Info size={20} className="text-yellow-600 mt-0.5 shrink-0" />
                <p className="text-xs text-yellow-700 font-medium">
                  Al cambiar estos IDs aquí, la App móvil actualizará sus bloques de anuncios en el siguiente reinicio, sin necesidad de enviar una nueva actualización a Google Play Console.
                </p>
              </div>
            </div>
          </section>

          {/* ZONA DE PELIGRO */}
          <section className="bg-red-50 p-8 rounded-[40px] border border-red-100">
            <h3 className="text-red-600 font-black text-xs uppercase tracking-widest mb-4 flex items-center gap-2">
              <AlertTriangle size={16} /> Danger Zone
            </h3>
            <div className="flex items-center justify-between">
              <div>
                <p className="font-bold text-red-900 text-sm">Modo Mantenimiento</p>
                <p className="text-xs text-red-700">Bloquea el acceso a la web y app temporalmente.</p>
              </div>
              <button className="bg-red-600 text-white px-4 py-2 rounded-xl text-xs font-bold hover:bg-red-700">
                Activar
              </button>
            </div>
          </section>

        </div>
      </div>
    </div>
  );
};

export default AdminSettings;