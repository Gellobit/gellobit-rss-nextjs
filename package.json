{
  "name": "gellobit-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "postcss": "^8",
    "turbo": "latest"
  },
  "packageManager": "npm@10.0.0"
}
